using UnityEngine;
using System.Collections;

public class CharacterMusic : MonoBehaviour {

	// Use this for initialization
	void Start () {
		MusicManager.Instance.characterchooseMusic ();
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
